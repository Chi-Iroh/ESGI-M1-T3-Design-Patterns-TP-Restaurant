info:
  name: Get order by ID
  type: http
  seq: 3

http:
  method: GET
  url: http://localhost:5000/api/orders/100
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
