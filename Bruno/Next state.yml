info:
  name: Next state
  type: http
  seq: 5

http:
  method: PUT
  url: http://localhost:5000/api/orders/100/state
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
